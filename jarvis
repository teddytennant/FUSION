#!/usr/bin/env bash
# JARVIS launcher script
# Resolves the repository directory of this script and invokes jarvis.py
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$SCRIPT_DIR/jarvis.py" "$@" 